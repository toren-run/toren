{
  "name": "toren-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b packages/cli/tsconfig.build.json && pnpm --dir packages/console build",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitepress": "^1.6.4",
    "vitest": "^2.1.0"
  },
  "packageManager": "pnpm@11.22.0"
}
